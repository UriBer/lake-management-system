# BigQuery Table List Extractor Configuration

# Default BigQuery Project ID
# This can be overridden using the --project command line argument
PROJECT_ID=your-project-id

# Optional: Set default dataset (can be overridden with --dataset argument)
# DEFAULT_DATASET=your-default-dataset
