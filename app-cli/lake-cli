#!/bin/bash
# Lake CLI wrapper script
# This script allows you to run 'lake-cli' from anywhere

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the actual lake_cli.py script
exec "$SCRIPT_DIR/lake_cli.py" "$@"
